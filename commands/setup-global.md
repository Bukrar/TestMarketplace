---
name: setup-global
description: 安裝公司標準的全域設定
---

# 安裝公司全域設定

請執行以下步驟：

1. 複製 `global-configs/CLAUDE.md` 到 `~/.claude/CLAUDE.md`
2. 複製 `global-configs/settings.json` 到 `~/.claude/settings.json`
3. 確認設定已生效

如果檔案已存在，詢問使用者是否要覆蓋。